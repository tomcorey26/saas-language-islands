---
alwaysApply: true
---

When creating forms, use the following rules:

- Use react-hook-form and zod for validation
- Use shadcn/ui form components

When validating action inputs, use the following rules

- Use zod for validation

DO:

- Create server actions
  - When creating a server action, validate the input with zod

DO NOT:

- Create nextjs api routes
